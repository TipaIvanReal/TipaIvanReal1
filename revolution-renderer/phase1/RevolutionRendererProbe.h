#pragma once

// Initializes a non-invasive Direct3D 12 capability probe for the Revolution renderer.
// The current WW3D draw path remains active while the modern backend is brought online.
void RevolutionRendererProbe_Initialize();
